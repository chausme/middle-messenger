<button
    type="button"
    title="{{{props.title}}}"
    class="button button-icon window sm p-0/5 pad d-flex align-items-center text-center shrink-0
        {{{props.styles}}}"
    id="{{{props.id}}}"
>
    <div class="inner d-flex">
        <img src="{{{icon}}}" alt="{{{props.title}}}" />
    </div>
</button>