<div class="window lg p-2/5 chats bg-pink d-flex h-100">
    <div class="inner">
        <header class="d-flex mb-2">
            {{{props.buttonIcon}}}
            {{{props.searchInput}}}
        </header>
        <div class="list">
            {{#each props.chats}}
                {{{this}}}
            {{/each}}
        </div>
    </div>
</div>